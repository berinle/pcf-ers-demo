---
applications:
- name: ((name))
  routes:
  - route: ((route))
  memory: 1GB
  instances: 1
  path: ((path))
  buildpack: ((buildpack))
  #services: [attendees-db]
  env:
    SPRING_PROFILES_ACTIVE: cloud
